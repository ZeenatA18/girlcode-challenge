const number = document.querySelector(".NameEntered");
const btn = document.querySelector(".button")

btn.addEventListener("click", () => {
    var storedNumber = list || []
    
        if ( storedNumber.push(personName)) {
            return true
        }

        
        

})